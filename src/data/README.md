# Data

This directory stores datasets for the ML lifecycle.

- `demo_metrics.csv`: Initial sample dataset collected from the k6 calibration run. Contains `caddy_http_requests_total`, `container_cpu_usage_seconds_total`, `container_memory_working_set_bytes`, `caddy_http_request_duration_seconds_bucket`, and `kube_deployment_status_replicas`.
